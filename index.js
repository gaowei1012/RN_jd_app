/**
 * @format
 */

import {AppRegistry} from 'react-native';
import BaseStores from './src/BaseStores';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => BaseStores);
